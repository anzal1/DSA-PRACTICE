#include <bits/stdc++.h>
using namespace std;

int inversions(int a[], int n,int l,int r)
{
    int m=(l+r)/2;
    if(l<r){}
}

int countmerge(int a[],int l,int m,int r )
{
    int n1=m-l+1;
    int n2=r-m;
    int left[n1],right[n2];
    for(int i=0;i<n1;i++)
    {}
}


//An inversion is a an element where i<j, a[i]>a[j]